<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LojaApp.Views.SidebarView"
             x:Name="sidebarRoot">
    <VerticalStackLayout Spacing="10" Padding="0">
        <!-- Sidebar Header/Logo -->
        <Image Source="dotnet_bot.png" HeightRequest="80" WidthRequest="80" HorizontalOptions="Center" Margin="0,20,0,20" />
        <Label Text="My App" FontSize="Title" FontAttributes="Bold" HorizontalOptions="Center" TextColor="White"/>
        <BoxView HeightRequest="1" Color="LightGray" Margin="0,10,0,10"/>

        <!-- Menu Items -->
        <Button Text="Nova Venda" Style="{StaticResource SidebarMenuItemStyle}" Clicked="OnMenuItemClicked" CommandParameter="Dashboard" />
        <Button Text="Cadastrar Produtos" Style="{StaticResource SidebarMenuItemStyle}" Clicked="OnMenuItemClicked" CommandParameter="Settings" />
        <Button Text="Historico de Vendas" Style="{StaticResource SidebarMenuItemStyle}" Clicked="OnMenuItemClicked" CommandParameter="Profile" />
        <Button Text="Relatorios" Style="{StaticResource SidebarMenuItemStyle}" Clicked="OnMenuItemClicked" CommandParameter="About" />
        <Button Text="Gerenciar Estoque" Style="{StaticResource SidebarMenuItemStyle}" Clicked="OnMenuItemClicked" CommandParameter="About" />
        <Button Text="Cadastrar Servico" Style="{StaticResource SidebarMenuItemStyle}" Clicked="OnMenuItemClicked" CommandParameter="Servico" />

        <!-- You can also use other layouts like CollectionView for dynamic menus -->
        <!-- <CollectionView ItemsSource="{Binding MenuItems}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10">
                        <Label Text="{Binding Title}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView> -->
    </VerticalStackLayout>
</ContentView>
